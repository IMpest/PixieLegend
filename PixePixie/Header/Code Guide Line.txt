程序进入大规模的开发准备期，在此设定一些有关代码风格相关的标准，
方便大家统一规范从而更加快捷的进行沟通以及增加程序的可读性。
（目前是草案，已经写糟的程序后续再改，后边的代码尽量严格，内容可商议)

- 所有的类名统一使用PP前缀命名
- 指针*号的使用遵循 NSString *str 这样的格式
- 左大括号{另起一行，右大括号}后不跟代码段
- 赋值语句左右留一个空格 i = 1;
- 逗号,后边保留一个空格
- 连续空行不能超过两行

